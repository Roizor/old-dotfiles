#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '

alias cl="git clone https://github.com/$1"
alias aiopen="chmod +x $1 && $1"

# CLI tools
export PATH="$PATH:`pwd`/flutter/bin"